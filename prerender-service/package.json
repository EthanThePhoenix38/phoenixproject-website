{
  "name": "phoenix-prerender-service",
  "version": "1.0.0",
  "description": "Service de pre-rendering SEO pour The Phoenix Agency",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "docker:build": "docker build -t phoenix-prerender .",
    "docker:run": "docker run -p 3000:3000 --env-file .env phoenix-prerender"
  },
  "dependencies": {
    "express": "^4.18.2",
    "puppeteer": "^21.6.0",
    "redis": "^4.6.11"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
